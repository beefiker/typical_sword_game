# 브라우저 검증 기록

- 초기 화면은 다크 Geist 콘솔 스타일로 렌더링되며, 제한 팔레트 중 시안은 주요 CTA와 진행 상태, 골드는 골드/보상 계열 카드 테두리, 주황·핑크는 위험/전투 계열 테두리에 사용된다.
- 상단에는 브랜드, 자동 전투 상태, 초기화 버튼이 표시된다.
- 히어로 영역에는 검 시각 자산이 정상 표시되고, 좌측에는 장기 방치형 RPG 설명과 기본 강화/직접 공격 CTA가 표시된다.
- 핵심 스탯 카드(골드, 검 강화, 공격력, 플레이어, 명성, 처치)가 정상 표시되며 자동 전투로 골드와 처치 수가 증가하는 것이 확인되었다.
- 탭 전환 검증에서 `지역` 탭 클릭 후 지역 목록이 표시되었고, 해금/잠김 상태와 워프권 사용 버튼이 정상적으로 나타났다.
- 현재 뷰포트 기준 하단 미리보기 안내 바가 일부 하단 콘텐츠를 가리지만, 이는 개발/미리보기 환경의 고정 안내이며 실제 배포 화면에서는 제거될 가능성이 높다.

추가 검증에서는 브라우저 콘솔에 오류 출력이 없는 것을 확인했다. 탭 패널 전환도 계속 동작했으며, 조합 패널에서 보유 재료, 방지권·안정화석·워프권 제작 버튼이 정상적으로 표시되었다. 자동 전투 루프는 검증 중에도 계속 진행되어 처치 수와 골드가 증가했다.


## 모바일 검증 추가 기록

390px 폭 헤드리스 캡처에서 모바일 전용 카드 압축, 2열 스탯, 하단 탭 구조는 적용되었으나 일부 상단 액션과 히어로 버튼 영역이 오른쪽 가장자리에서 잘리는 현상이 확인되었다. 원인은 작은 화면에서 패딩과 보더가 요소 폭 계산에 포함되지 않는 기본 `content-box` 모델 및 일부 플렉스/그리드 자식의 최소 폭 유지로 판단된다. 전역 `border-box` 적용, 모바일 상위 컨테이너의 `overflow-x` 차단, 버튼/타이틀의 최소 폭 축소를 추가로 적용해야 한다.


## 모바일 우선 재구성 검증

390px 폭 기준 헤드리스 캡처에서 데스크톱형 2열 콘솔 느낌을 줄이고, 첫 화면 핵심 조작을 세로 흐름으로 재배치했다. 히어로 CTA는 한 손 조작을 우선하여 `기본 강화`와 `직접 공격`을 각각 전체 폭 버튼으로 정리했고, 히어로 제목은 작은 화면에서 잘리지 않도록 의미 단위 세 줄 구조로 바꾸었다. 스탯 카드는 모바일에서 2열 카드로 유지하되 내부 수치와 라벨이 넘치지 않도록 폭 제한, `box-sizing`, 텍스트 축약 규칙을 추가했다.

빌드 검증 결과 `pnpm check`와 `pnpm build`는 통과했다. 프로덕션 번들 크기 경고는 존재하지만 런타임 오류는 아니며, 현재 단일 페이지 게임 구조에서 기능 검증을 막는 문제는 아니다.


## 저장 및 핵심 조작 검증

미리보기 환경에서 `기본 강화` CTA를 눌렀을 때 검 강화 수치가 +0에서 +1로 상승하고, 골드가 강화 비용만큼 차감되며 공격력과 다음 강화 확률/비용이 즉시 갱신되는 것을 확인했다. `직접 공격` CTA는 별도 오류 없이 입력을 받았고, 자동 전투 루프는 계속 진행 중이었다.

저장 시스템은 `localStorage`의 `typical-sword-game:v1` 키에 골드, 플레이어 레벨, 경험치, 강화 수치, 최고 강화, 처치 수, 지역, 몬스터 체력, 인벤토리 등의 진행 상태를 JSON 형태로 기록하고 있었다. 따라서 새로고침 이후에도 단일 브라우저 기준 진행 상태가 유지되는 구조가 정상 동작한다.
